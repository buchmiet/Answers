mode: Mainline
assembly-versioning-scheme: MajorMinorPatch
assembly-file-versioning-scheme: MajorMinorPatch
increment: Patch

branches:
  main:
    regex: ^main$
    increment: Patch
    is-mainline: true
  develop:
    regex: ^dev(elop)?(/.+)?$
    increment: Minor
  feature:
    regex: ^features?[/-]
    increment: Inherit
ignore:
  sha: []
